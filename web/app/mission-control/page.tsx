import MissionControl from "./controller";

export const metadata = {
  title: "Demo Mission Control · Reality Sync",
  description: "Watch evidence become an approved, verified repair.",
};

export default function Page() {
  return <MissionControl />;
}
