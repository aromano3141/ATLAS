# Reality Sync interface

This local Vinext/React interface uses the bundled Sites component primitives.
Start the complete application from the repository root with npm run dev.
API requests are proxied to the local Hono service on port 4318. This application
is not deployed. See the root README and docs/SETUP.md for provider configuration.